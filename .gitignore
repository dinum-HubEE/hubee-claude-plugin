# Handoff Claude → dev pour `git commit` (cf. skill commit du plugin)
.commit-msg*.tmp
.commit-files*.tmp
.pr-body*.tmp

# Résidu de découpage de commits/MR (ne doit jamais être versionné)
.split-changes.patch

# Superpowers (plans/specs générés à chaque session, pas du contenu plugin)
docs/superpowers/
